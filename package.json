{
	"name": "haxedci-example",
	"version": "1.0.0",
	"repository": {
		"type": "git",
		"url": "https://github.com/ciscoheat/haxedci-example"
	},
	"dependencies": {
	},
	"scripts": {
		"dependencies": "npm install -g npm-run-all http-server",
		"server": "http-server bin -c-1 -o",
		"start": "npm-run-all --parallel server"
	}
}